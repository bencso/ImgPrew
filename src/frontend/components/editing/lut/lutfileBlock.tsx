import { Box, Button, CloseButton, FileUpload, Input, InputGroup, Span, Text } from "@chakra-ui/react";
import { LuFileUp } from "react-icons/lu";

export default function LUTFileBlock() {
    return (
        <Box>
            

            <Button colorPalette={"teal"} variant={"subtle"} mt={3}>Mentés</Button>
        </Box>
    )
}